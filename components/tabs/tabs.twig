{% set classes = [
  'tabs',
  'has-rhythm',
  placement,
  size,
  style|default('tabs-border'),
  canvas_is_preview ? 'min-h-20',
] %}

<div{{ attributes.addClass(classes).setAttribute('data-tabs-id', canvas_uuid) }}>
  {% block items %}{% endblock %}
</div>
