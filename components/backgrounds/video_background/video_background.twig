{# Set default values for props to prevent errors #}
{% set url = url|default(null) %}
{% set media_query = media_query|default(null) %}

{% set classes = [
  'video-background',
  'absolute',
  'inset-0',
  'overflow-hidden',
  opacity ? 'opacity-' ~ opacity,
] %}

{# Only render if there is a URL to display #}
{% if url %}
  <div{{ attributes
    .addClass(classes)
    .setAttribute('data-video-background', true)
    .setAttribute('data-video-background-url', url)
    .setAttribute('data-background-media-query', media_query ? media_query : null) }}>
  </div>
{% endif %}
